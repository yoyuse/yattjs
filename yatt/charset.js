function Charset(id, title, chars, option = {certain: true, uncertain: true}) {
    this.id = id;
    this.title = title;
    this.chars = chars;
    this.option = option;
}

const charsets = new Array();
